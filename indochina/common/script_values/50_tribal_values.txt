feudalize_tribal_holding_interaction_cost = {
	value = 300
	if = {
		limit = {
			holder = {
				is_ai = yes
				capital_province = prev.title_province
			}
		}
		add = -150
	}
	multiply = { # Reduce total cost by 2% per point of development in the county, max 50% discount
		value = 1
		subtract = {
			value = scope:target.development_level
			divide = 100
		}
		min = 0.5
	}
}