#To overwrite More Game Rules by Neutrino's game rule, to prevent confusion when using together with MGR as Fervour Midpoint Rebalance has it's own heresy spawn mechanism
heresy_spawn = {
	default = fmr_heresy_spawn
	
	fmr_heresy_spawn = {
	}
}