# root: is involved (or interloper) character
# Scope:struggle is the currently pulsing struggle.
# yearly_struggle_playable_pulse = {}

# root: is involved (or interloper) character
# Scope:struggle is the currently pulsing struggle.
on_game_start = {
	on_actions = {
		lock_indo_greek
	}
}

lock_indo_greek = {
	effect = {
		faith:indo_hellenism = { set_variable = { name = block_conversion_till_decision_taken } }
	}
}

#ROOT = struggle
calc_dominance = {
	effect = {
		#setup dominance variables
		if = {
			limit = {
				OR = {
					NOT = { has_variable = hinduism_religion_BP }
					NOT = { has_variable = buddhism_religion_BP }
					NOT = { has_variable = jainism_religion_BP }
					NOT = { has_variable = islam_religion_BP }
					NOT = { has_variable = indian_religion_BP }
					NOT = { has_variable = foreign_religion_BP }
				}
			}
			set_variable = { name = hinduism_religion_BP value = 0 }
			set_variable = { name = buddhism_religion_BP value = 0 }
			set_variable = { name = jainism_religion_BP value = 0 }
			set_variable = { name = islam_religion_BP value = 0 }
			set_variable = { name = indian_religion_BP value = 0 }
			set_variable = { name = foreign_religion_BP value = 0 }
		}
	}
}
